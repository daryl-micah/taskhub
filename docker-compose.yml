services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: th_dev
      POSTGRES_PASSWORD: password
      POSTGRES_DB: taskhub
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
